<Application x:Class="View_AP.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:View_AP"
             xmlns:view_model="clr-namespace:VM_AP;assembly=VM_AP"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <Style x:Key="RegistrationtWindow" TargetType="{x:Type FrameworkElement}">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Control.Height" Value="30"/>
            <Setter Property="Control.Width" Value="400"/>
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <DockPanel LastChildFill="True">
                            <Border BorderBrush="Red" BorderThickness="1" Padding="8,3,8,3" CornerRadius="12"  DockPanel.Dock="Right" Margin="10,0,0,0">
                                <TextBlock  Foreground="Red" FontSize="23" FontWeight="ExtraBold" FontFamily="Times New Roman"  ToolTip="{Binding ElementName=adornerPlaceholder, Path=AdornedElement.(Validation.Errors)[0].ErrorContent}">!</TextBlock>
                            </Border>
                            <AdornedElementPlaceholder Name="adornerPlaceholder"></AdornedElementPlaceholder>
                        </DockPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource RegistrationtWindow}"/>
        <Style TargetType="{x:Type Label}" BasedOn="{StaticResource RegistrationtWindow}"/>

        <Style TargetType="{x:Type Button}">
            <Setter Property="Padding" Value="20,2"/>
            <Setter Property="Foreground" Value="#660010"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="GhostWhite" CornerRadius="3" BorderBrush="#660010" BorderThickness="2,1,1,2">
                            <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="White" />
                                <Setter Property="Foreground" Value="#A6A6A6"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#EBE5E5" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,3,3,1" />
                                <Setter TargetName="Border" Property="Background" Value="#DECECE" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="BtnProductNameTemplate" TargetType="{x:Type Button}">
            <Border Background="Transparent">
                <TextBlock x:Name="textBlock" Margin="{TemplateBinding Padding}" Text="{Binding Path=ProductName}" FontSize="24" TextDecorations="{x:Null}"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="textBlock" Property="TextBlock.TextDecorations" Value="Underline" />
                    <Setter TargetName="textBlock" Property="TextBlock.FontSize" Value="25" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        
    </Application.Resources>
</Application>
